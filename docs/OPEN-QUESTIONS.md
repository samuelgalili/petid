# שאלות פתוחות — petid

כל מה שממתין להחלטה שלך, במקום אחד. נכון ל-2026-09-14, קצה פרודקשן `6a735bb8`.

**איך להשתמש:** עונים כשנוח, לא בבת אחת. לכל שאלה יש המלצה שלי ומה היא חוסמת,
כדי שתוכל לענות על מה שדחוף ולהשאיר את השאר.

**סימון:** 🔴 חוסם עבודה · 🟡 משפיע על משתמשים · ⚪ חוב

---

## א. שאלות מוצר — רק אתה יכול לענות

### Q-01 · עיר הבעלים במודעת אבידה 🟡
`F-008`. השאילתה הציבורית בודקת `show_location === true` כדי להחליט אם להציג את
עיר הבעלים. שום דבר לא כותב לעמודה, אז היא תמיד `NULL` והעיר לעולם לא מוצגת.
אין דליפה — ברירת המחדל בטוחה — אבל הפקד לא קיים.

**השאלה:** בעלים שמסמן את החיה כאבודה אמור להיות מסוגל להציג את עירו?
**המלצה:** כן — במודעת אבידה עיר היא מה שמחזיר חיה הביתה. פקד אחד ב-`EditPet`.
**חוסם:** כלום. אפשר לדחות.

### Q-02 · מחיקת חיה מייתמת מסמכים 🟡
`F-006`. מחיקת חיה מוחקת ב-CASCADE את החיסונים, ביקורי הווטרינר והדמות — אבל
**המסמכים הרפואיים, תביעות הביטוח, ההזמנות והרגעים שורדים מנותקים**
(`SET NULL`). הם נשארים בבעלות המשתמש וקריאים, אבל שום דבר לא אומר לאיזו חיה
הם שייכים, ואף אחד לא מודיע לבעלים שזה קרה.

**השאלה:** "לשמור את הרשומות" או "הבעלים ביקש שזה ייעלם"?
**המלצה:** לשמור, אבל **להגיד לבעלים לפני המחיקה** מה ישרוד ומה ימחק.
**חוסם:** Journey P ב-`21-ERROR-RECOVERY.md`.

### Q-03 · מערכת ביקורות למוצרים ⚪
`F-012`. `average_rating`, `review_count` קיימות ב-`business_products` ואין
טבלת ביקורות ואין כותב. מספרי הדירוג היחידים בקוד קשיחים ב-`AIAnalytics.tsx`,
שאין לו importers.

**השאלה:** נבנית מערכת ביקורות?
**המלצה:** אם לא ברבעון הקרוב — למחוק את שתי העמודות.

### Q-04 · ספקים חיצוניים ⚪
`F-013`. `business_profiles` מכילה 10 עמודות מתות שמתארות מדריך ספקים — שעות
פתיחה, שירותים, טווח מחירים, וזרימת אישור בת שלוש עמודות בלי מאשר. יש שורה
אחת בטבלה, "Mipo Shop", בלי CRUD ובלי מסכים.

**השאלה:** Mipo תציג ספקים חיצוניים?
**המלצה:** אם כן — הסכמה הזו נקודת פתיחה סבירה, להשאיר. אם לא — למחוק.

### Q-05 · אימות דו-שלבי לאדמין 🔴
ענף `claude/admin-2fa` מחזיק שני קומיטים שלא נדחפו מאז 6 בספטמבר. לפי גוף
הקומיט של `532f3abd` הוא דורש `SECRET_ENCRYPTION_KEY` ב-SSM, **ובלעדיו ה-API
מסרב לעלות בפרודקשן**.

**השאלה:** מפעילים 2FA לאדמין?
**המלצה:** כן. אבל המפתח נכנס דרך עמוד אדמין מוצפן — **לעולם לא בצ'אט,
במייל או ב-WhatsApp**, כי שם הוא נשאר בתמלול.
**חוסם:** את הענף כולו.

---

## ב. החלטות הנדסיות — יש לי ברירת מחדל, צריך אישור

### Q-06 · DUAL WRITE — הצעד הגדול הבא 🔴
`F-005`. שכבת העובדות נפרסה ואין לה כותבים. הקוראים היחידים ל-`writeFact`
ול-`recordObservation` הם שני ה-routes של ה-API עצמם: הוספת חיה, סריקת מסמך
ועדכון משקל כותבים **אפס עובדות**.

**השאלה:** איזה מפתח מחברים ראשון, ומתי?
**המלצה:** **משקל.** כבר יש לו backfill (`0039`), הגדרה ברישום, ורשומה
ב-`FACT_PRODUCING_OBSERVATIONS`. מאותו רגע יש היסטוריית משקל אמיתית במקום
מספר שנדרס.
**חוסם:** כל ההבטחה של Pet 360. **זה הפריט היחיד ברשימה שמשנה מה המערכת יודעת.**

### Q-07 · מחיקת 45 העמודות המתות ⚪
`F-007`, `F-012`, `F-013`. 45 מתוך 224 העמודות שמופו אינן נכתבות ואינן נקראות.

**השאלה:** למחוק?
**המלצה:** **לא לפני ספירת נתונים.** `legacy_auth_provider`, `legacy_user_id`
ו-`imported_at` הן עמודות ההגירה מ-Supabase, ו-`points`, `interests`,
`favorite_breeds` עשויות להחזיק נתוני משתמשים אמיתיים מאותה תקופה. **מחיקה
בלתי הפיכה, ואי אפשר לראות מכאן מה יש בהן בפרודקשן.**
**המסלול:** לספור שורות לא-ריקות → להחליט לכל עמודה → לגבות → מיגרציה אחת
עם rollback בכותרת.

### Q-08 · פורמט תאריכים ב-API 🟡
`pg` מחזיר עמודות `date` כאובייקט `Date`, אז ה-API שולח
`"2027-01-15T00:00:00.000Z"` במקום `"2027-01-15"`. נוגע ל-`birth_date`,
שני תאריכי הווטרינר ושני תאריכי התפוגה.

**השאלה:** לתקן?
**המלצה:** כן, אבל **בזהירות** — לקוחות קיימים אולי מסתמכים על הפורמט הנוכחי.
תיקון אומר לבדוק כל צרכן.

### Q-09 · תאריך לידה עתידי של אדם ⚪
תיקנו את זה לחיה. במסלולי ההרשמה ועדכון הפרופיל **תאריך לידה של אדם עדיין
מתקבל בעתיד**.
**המלצה:** לתקן — אותה בדיקה בדיוק, שתי שורות.

### Q-10 · 13 קבצי e2e שמעולם לא רצו 🔴
20 קבצי spec, **7 בלבד** תואמים ל-`testMatch: "**/*.aws.spec.ts"`. ביניהם
`add-pet.spec.ts` ו-`edit-pet.spec.ts` — בדיוק הטפסים שתיקנו, ששינויי
הקליינט בהם **לא נבדקו בהרצה** עד היום.

**השאלה:** לפתוח את כולם, או רק את שני טפסי החיה?
**המלצה:** קודם את שני הטפסים, לראות שהם עוברים, ואז להחליט על השאר.
**חוסם:** כל שינוי קליינט עתידי נשאר לא מאומת.

### Q-11 · ה-outbox לא מוסר כלום 🟡
`F-004`. `AUTOMATION_WEBHOOK_URL` אינו ב-`REQUIRED_KEYS` ולא ב-`OPTIONAL_KEYS`
של `deploy/aws/sync-ssm-env.sh`. בלי הכתובת `startDispatcher` מחזיר `() => {}`
ואירועים נצברים בלי להימסר.

**השאלה:** מחברים את n8n?
**המלצה:** כן — **שום דבר לא אבד**, וברגע שהמשתנה יוגדר כל מה שהצטבר יימסר.
**הערה:** שרת n8n מוגדר בסשן הזה אך **נכשל בחיבור**, אז לא יכולתי לבדוק מולו.

### Q-12 · שער אישור על פרודקשן 🟡
ה-job `deploy` קשור ל-`environment: production` וההערה בקוד מפנה
ל-`docs/DEPLOY_APPROVAL.md` — אבל ריצות 46–51 **לא נעצרו לאישור**. הסקה
מהתנהגות: אין reviewer מוגדר על הסביבה בהגדרות GitHub.

**השאלה:** להוסיף reviewer?
**המלצה:** כן. היום כל דחיפה ל-`aws-migration` יוצאת לאוויר לבד.
**הערה:** זו הגדרה ב-GitHub, מחוץ לריפו — רק אתה יכול לבצע.

---

## ג. ענפים שממתינים — 9 ענפים, 64 קומיטים שאינם בפרודקשן

| ענף | קומיטים | מה | מה נדרש |
|---|---|---|---|
| `claude/petaidy-code-review-be1706` | **31** | מעבר ביצועים — פיצול vendor chunks, טעינת xlsx על פי דרישה, מנוי auth אחד במקום אחד לכל רכיב | **Q-13:** רלוונטי אחרי חודש? |
| `claude/mipo-import-engine` | **19** | מנוע ייבוא, דחיפת אירועים ל-n8n, הצעת מחירים | **Q-14:** קשור ל-Q-11 |
| `claude/category-filter` | 4 | תיקון שער אישור ×3 | **Q-15:** מיותר אחרי Q-12? |
| `claude/product-page-spine` | 3 | אותם קומיטים | Q-15 |
| `claude/admin-2fa` | 2 | 2FA לאדמין | Q-05 |
| `claude/workbench` | 2 | אותם קומיטים | Q-15 |
| `claude/activity-audit` | 1 | Phase 0 ל-Activity Network | **Q-16:** להמשיך? |
| `claude/deploy-safety` | 1 | אישור על הסביבה שמחזיקה את המפתח | Q-12 |
| `main` | 1 | מ-2026-06-29 | ⚪ `main` אינו ענף הפריסה |

**הערה:** שני הענפים הראשונים לא היו ביומן עד עכשיו — 50 קומיטים של עבודה
אמיתית מאוגוסט. שווה החלטה לפני שהם מתיישנים עוד.

---

## ד. תחזוקת תיעוד

### Q-17 · 120 הפניות `קובץ:שורה` ⚪
מתיישנות בכל עריכה. `03-PET-LIFECYCLE.md` הוא היחיד שכבר נקי — כולן הוחלפו
בעוגנים שניתן לחפש.
**המלצה:** לתקן תוך כדי מעבר על כל מסמך, לא במכה אחת.

### Q-18 · שתי סדרות החלטות במקביל ⚪
`D-01`…`D-18` ב-`system-workflows/DECISIONS.md` ו-`DD-01`…`DD-18`
ב-`pet-intelligence`. מבלבל.
**המלצה:** להכריז על אחת כקנונית ולהפנות מהשנייה.

### Q-19 · תבנית WF/DF/SC ל-30 המסמכים הנותרים ⚪
מיושמת במסמך אחד. **שאלה:** להחיל על כולם, או רק על מה שנוגעים בו?
**המלצה:** רק על מה שנוגעים בו. תיעוד שאיש לא קורא לא שווה את המעבר.

### Q-20 · סקריפט הטענות ב-CI ⚪
הסכמנו לבנות אחרי המיפוי. המיפוי הסתיים.
**המלצה:** לבנות — הוא הופך את התיעוד ממשהו שנרקב למשהו שה-build בודק.

---

## סיכום

| קטגוריה | שאלות | חוסמות |
|---|---|---|
| מוצר | Q-01 … Q-05 | Q-05 |
| הנדסה | Q-06 … Q-12 | Q-06, Q-10 |
| ענפים | Q-13 … Q-16 | — |
| תיעוד | Q-17 … Q-20 | — |
| **סה״כ** | **20** | **3** |

**אם עונים רק על אחת: `Q-06`.** DUAL WRITE הוא הפריט היחיד ברשימה שמשנה מה
המערכת יודעת על חיה. כל השאר ניקיון או תשתית.
